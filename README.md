# ProcessMonitor
ProcessMonitor is a lightweight, hardware-aware profiler for edge devices. It captures 100ms snapshots of CPU, RAM, and threads using psutil. By tracking CPU variance, it identifies bursty behavior that triggers DVFS and thermal throttling, helping engineers optimize performance on resource-constrained hardware via ADB.
